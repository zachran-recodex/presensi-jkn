APP_NAME="Website Presensi - PT. Jaka Kuasa Nusantara"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://jakakuasanusantara.web.id

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# Database Configuration (Docker)
DB_CONNECTION=mysql
DB_HOST=database
DB_PORT=3306
DB_DATABASE=presensi_jkn
DB_USERNAME=jkn_user
DB_PASSWORD=Zachran#recodex15

# Cache & Session (Redis)
BROADCAST_DRIVER=redis
CACHE_DRIVER=redis
FILESYSTEM_DISK=public
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120

# Redis Configuration
REDIS_HOST=redis
REDIS_PASSWORD=redis_password_jkn
REDIS_PORT=6379

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=localhost
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@jakakuasanusantara.web.id"
MAIL_FROM_NAME="${APP_NAME}"

# AWS Configuration (if needed for S3 storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=ap-southeast-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher Configuration (if using real-time features)
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=ap1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Biznet Face Recognition API Configuration
BIZNET_FACE_BASE_URL=https://fr.neoapi.id/risetai/face-api
BIZNET_FACE_ACCESS_TOKEN=tok_3zjeFwGN3Bqf1GrePCpHnEuc4s5zKyuf3RH7
BIZNET_FACE_GALLERY_ID=jkn.production
BIZNET_FACE_SIMILARITY_THRESHOLD=0.75
BIZNET_FACE_TIMEOUT=30

# Application Specific Settings
ATTENDANCE_PHOTO_RETENTION_DAYS=90
ATTENDANCE_MAX_DISTANCE_METERS=100
ATTENDANCE_ALLOW_LATE_CLOCK_OUT=true
ATTENDANCE_AUTO_CLOCK_OUT_ENABLED=false

# Security Settings
SESSION_SECURE_COOKIE=false
SESSION_SAME_SITE=lax
SANCTUM_STATEFUL_DOMAINS=jakakuasanusantara.web.id,www.jakakuasanusantara.web.id

# Logging Configuration
LOG_AUDIT_CHANNEL=audit
LOG_ATTENDANCE_CHANNEL=attendance
LOG_FACE_RECOGNITION_CHANNEL=face_recognition

# Performance Settings
OCTANE_ENABLED=false
TELESCOPE_ENABLED=false
